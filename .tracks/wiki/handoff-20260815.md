# Handoff — 2026-08-15 08:10 JST

> 值夜会话收尾。run 卡在 escalation 待人工裁定；本文件含状态快照、根因线索、恢复步骤。

## 1. 当前状态（最重要）

```
run=01KZTHE7RMZE6110PK9C54K1E2  stage=M-TEST  substate=WRITE
status=awaiting_human  awaiting=escalation  attempts=3
reason=[over_reach] over-reach detected; agent changes rolled back
```

- **monitor2 已退出**（08:00 JST 窗口结束，最后日志：`gate persists (sig=66f5936f4103 already retried once) — holding for human`）—— 现在无自动监控/充值/重启。
- 服务器：`mini-one-vpn`，仓库 `~/workspace/tracks`，所有日志在 `logs/`。

## 2. 失败时间线（UTC，JST=+9）

| UTC | 事件 |
|---|---|
| 20:31 | Shield attempt 2 dispatch（Prism R2 verdict=revise 后重派） |
| 21:26 | attempt 2 失败 `manifest_malformed: suggested_commit_message must be a non-empty string`（3318s 白跑） |
| 21:26 | attempt 3 dispatch → 21:33 失败 `over_reach`(426s) → **escalation** |
| 21:35 | autopilot 完成排队部署（安全窗口判断正确） |
| 21:36–22:30 | monitor2 自动重启 loop 后**连续 3 轮全部 over_reach**（996s / 1519s / 719s）→ 再次 escalation |

根因见下节。

## 3. 根因（已查明，事件 seq675 blob 的 audit_evidence）

```
over-reach: pyproject.toml, tests/_doc_gap_hook/sitecustomize.py, tests/doc_gap_injection.py
```

**机制性连锁**（2026-08-15 早班复核有两处技术错误，已勘误）：
1. ~~manifest 解析失败路径不做回滚/清理，半成品留在 working tree~~ **勘误**：`_manifest_for_dispatch` 在审计前早退（opencode.py:207-216），失败路径确实不进审计/回滚——但"残留污染下一轮"不成立：每轮 dispatch 的 baseline 是**重新快照**的（audit.py:118），上轮 untracked 残留进入本轮 baseline、不判 over_reach。doc_gap 两文件反复出现，是 **Shield 每轮按 Prism R2 findings 重写同一批文件**，不是残留跨轮污染。
2. Shield 试图改 `pyproject.toml`：为超长行加 lint 豁免（和值班代理 deploy 做的是同一件事）——pyproject 不在 [layout.shield] 白名单 → over_reach。另注：Shield WRITE over_reach 本可走 `adjudicate()`（Prism ALLOW/DENY 豁免，opencode.py:502-505），但 pyproject 属真越权，DENY 合理。
3. 连耗尽的真实教训：**同根因 over_reach 下 retry 只会重烧预算**。monitor2 的 sig 单次 retry 不区分新根因与复发，over_reach 连发时应在首次复发后 hold。

**暴露的两个真缺陷**（值得进 decisions/spec）：
- **失败残留不清理**：manifest_malformed / non_zero_exit 等失败路径不回滚 agent 半成品 → untracked 跨轮污染 → 下轮误判 over_reach。一次 manifest 格式错误引发无限 over_reach 循环。
- **Shield 缺"构建配置只读"约定**：它聪明地发现 lint 会挡 commit 并尝试自救，但没人告诉它 pyproject 不可写、超长行 runtime 会兜底。

（另：monitor2 的重启被记为 `human.retry` 事件，seq 656/669。）

## 4. 恢复步骤（建议顺序）

1. **清理失败残留**（root cause 消除）：
   ```bash
   cd ~/workspace/tracks
   rm tests/_doc_gap_hook/sitecustomize.py tests/doc_gap_injection.py   # attempt 2 半成品
   git diff pyproject.toml | head   # 若与 ruff 豁免一致 → git add pyproject.toml && git commit（保留豁免）
   git diff .tracks/projects/v0.5/test-plan.md | head   # 先看内容再决定：Prism 线程更新则保留，Shield 越权正文编辑则 checkout --
   git status   # 确认只剩 Shield tests 文件（M tests/...）
   ```
2. **修 Shield 规则**（防复发）：`tracks/agents/Shield.md` + `.opencode/agents/Shield.md` 加两条 —— 构建配置（pyproject/.githooks/tox 等）只读，lint 拦截留给 runtime；manifest 提交前自检 `suggested_commit_message` 非空。
3. **修 runtime 缺陷**（可选，中期）：failed outcome 路径（manifest_malformed/non_zero_exit）也执行 rollback 清理，防止半成品跨轮污染。此条建议记 decisions（D-37）。
4. **处理 escalation**：查 `.venv/bin/trac --help` 找 return/resume 命令裁定重试；或直接重启 loop（重放会回到 M-TEST WRITE，重新 dispatch）。
5. **重启监控**：`nohup sh ~/workspace/tracks/logs/night-monitor2.sh &`（内置 08:00 窗口逻辑已到期，需改窗口参数或直接跑）。
6. 观察 attempt 恢复；M-TEST 通过进 M-IMPL 时记得 D-35 触发条件（复核代码评审锚定通道）。

## 5. 今晚已完成并提交（git log 可查）

- `f8cd983` FR-11 修复（machine.py `last_failure` + result_checkpoint.py 透传，18 行）—— revise 重派 evidence 带评审意见
- ruff tests E501 豁免（pyproject，**未提交**，见上）
- SC-D35 文档部署：`.tracks/wiki/spec-d35-review-persistence.md`（FR-0240 + AC-FR0240-01..06）
- decisions.md：D-36（OOB 提交机制：浅版=静默期 re-baseline + Tracks-OOB trailer + oob.accepted 事件；Agent 无 git 权限，逃逸不作约束）+ D-34/35/36 标 ◆=backlog 图例
- 此前：`f10dca3`（M-TEST reviewer 死锁修复）、`b880c6b`/`afb6578`（D-35）、`12df93b`（D-34）、`c5ceccc`（PTY 死锁）、`1e96170`（测试桩）

## 6. 本地文件（本工作区根目录）

| 文件 | 内容 |
|---|---|
| night-progress.log | 全夜时间线 + 机制分析（over_reach/baseline/红线规则在 41-43 行） |
| deploy-pending.sh | 已执行完的部署脚本（留档） |
| night-autopilot.sh | 自动部署器（已退出，pid 579） |
| spec-d35-review-persistence.md | SC-D35 原件（副本已在远程 wiki） |
| handoff.md | 本文件 |

远程 `~/deploy-staging/` 有上述脚本副本。

## 7. 机制要点（读过的代码结论，省你重查）

- **over_reach = `agent_changed_paths(baseline)`**：baseline 为**每次 dispatch 前**重新快照（audit.py:118）；clean→changed 或 pre-dirty 内容漂移都算 Agent 写；回滚对 pre-dirty 路径按**快照字节**恢复（非 checkout HEAD），Human 未提交内容不丢。**dispatch 窗口内不可改白名单外文件**；上轮 untracked 残留进入本轮 baseline 后不再判 over_reach（残留污染不是 over_reach 连发机制）。
- `.githooks/pre-commit`：ruff 扫 `tracks tests` 全仓（与 staged 无关），无 --no-verify；runtime checkpoint commit 也走它。
- events 表 1510+ 行，payload 行内 ≤8KB，超出自动 `$ref` 到 `runtime/blobs/`（137MB/568 文件）。
- 充值：new-api `POST /api/user/manage` add_quota；今晚自动 $300 + 手动 $600，TOPUP_CAP 曾设 $3000；余额查 `GET /api/user/self`（root）。

## 8. 成本提示

Shield 今晚白跑的 token 不小：3318s + 426s + 996s + 1519s + 719s ≈ 116 分钟 agent 时间因 manifest/over_reach 报废。恢复前建议先查 new-api 余额再决定是否继续跑。
