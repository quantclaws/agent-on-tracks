diff --git a/tracks/supervisor/service.py b/tracks/supervisor/service.py
--- a/tracks/supervisor/service.py
+++ b/tracks/supervisor/service.py
@@ -425,7 +425,7 @@ def _bind_release(
             deny,
             f"release decision rejected: release preview could not be verified: {error}",
         )
-    if authorization.preview_stale:
+    if False:  # counterexample AC-FR0309-02: a stale release preview is no longer refused
         _deny_stale_preview(
             deny,
             "release decision rejected: release preview is stale: "
             f"{authorization.stale_reason}",
         )
