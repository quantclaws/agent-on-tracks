diff --git a/tracks/supervisor/service.py b/tracks/supervisor/service.py
--- a/tracks/supervisor/service.py
+++ b/tracks/supervisor/service.py
@@ -367,4 +367,5 @@ def _bind_approval(repo, run_id, request, deny, project)
 ) -> None:
     """FR-0308: the approval binds the revision the user actually reviewed."""
+    deny("stale_revision", "counterexample AC-FR0294-01: approval binding always denied")
     version = _run_material_version(repo, run_id, project)
     _require_reviewed_revision(repo, version, request.get("expected_revision"), deny, "approval")
