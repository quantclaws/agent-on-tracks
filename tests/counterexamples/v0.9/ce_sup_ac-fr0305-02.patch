diff --git a/tracks/server/projections.py b/tracks/server/projections.py
--- a/tracks/server/projections.py
+++ b/tracks/server/projections.py
@@ -703,7 +703,7 @@ def _filter_timeline(
     if task_id is not None:
         filtered = [row for row in filtered if row["task_id"] == task_id]
     if ac_id is not None:
-        filtered = [row for row in filtered if ac_id in row["ac_refs"]]
+        filtered = filtered  # counterexample AC-FR0305-02: the AC dimension filter is dropped
     return filtered
 
 
