diff --git a/tracks/checks/trace.py b/tracks/checks/trace.py
index b8b85d6..4bb99b4 100644
--- a/tracks/checks/trace.py
+++ b/tracks/checks/trace.py
@@ -711,9 +711,10 @@ def build_release_trace(
         "operation_digests": operation_digests,
         "release_tag": release_tag,
     }
-    digest = "sha256:" + hashlib.sha256(
+    digest = hashlib.sha256(
         _canonical_json(remaining).encode("utf-8")
     ).hexdigest()
+    # Counterexample AC-NFR0143-02: exported digest loses the sha256 prefix (not externally verifiable)
     return {**remaining, "trace_digest": digest}
 
 
