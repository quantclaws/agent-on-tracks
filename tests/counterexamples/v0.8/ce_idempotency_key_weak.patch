diff --git a/tracks/executor/publish.py b/tracks/executor/publish.py
index 53609bd..350fb38 100644
--- a/tracks/executor/publish.py
+++ b/tracks/executor/publish.py
@@ -71,7 +71,8 @@ def operation_idempotency_key(preview_digest: str, kind: str, target: str) -> st
         separators=(",", ":"),
         ensure_ascii=False,
     ).encode("utf-8")
-    return "sha256:" + hashlib.sha256(raw).hexdigest()
+    del raw, preview_digest  # Counterexample AC-NFR0144-01: weak key without preview binding
+    return "weak:" + kind + ":" + target
 
 
 def reconcile_operation(planned: dict, remote_state: dict) -> ReconcileVerdict:
