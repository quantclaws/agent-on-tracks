diff --git a/tracks/executor/failure_review.py b/tracks/executor/failure_review.py
index 0064f71..2190abc 100644
--- a/tracks/executor/failure_review.py
+++ b/tracks/executor/failure_review.py
@@ -29,7 +29,8 @@ def next_failure_id(run_id: str, round_no: int, source: str) -> str:
 
 
 def record_failure(run_id: str, round_no: int, source: str, record: dict) -> dict:
-    fid = _next_failure_id(run_id, round_no, source)
+    # Counterexample AC-FR0280-01: overwrite the un-ACKed rich record identity
+    fid = f"{round_no}-{source}-0"
     entry: dict = {
         "failure_id": fid,
         "run_id": run_id,
