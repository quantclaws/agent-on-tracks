diff --git a/tracks/executor/repair.py b/tracks/executor/repair.py
index a18ff95..256326a 100644
--- a/tracks/executor/repair.py
+++ b/tracks/executor/repair.py
@@ -175,7 +175,7 @@ def register_known_issue(repo, attribution: dict, candidate_sha: str) -> dict:
     task_id = attribution.get("task_id")
     if not item and ac_refs:
         item = ac_refs[0]
-    if kind in _NOT_PRODUCT_DEFECT:
+    if kind in _NOT_PRODUCT_DEFECT and kind != "mechanism_failure":  # Counterexample AC-FR0286-06: mechanism wrongly registered
         # §1a row 29 pair payload: BOTH faces carry the closed members —
         # issue_number/url nullable on the rejected face, plus the pair label
         # and evidence refs; the rejection additionally carries reason.
