diff --git a/tracks/cli/release_cmd.py b/tracks/cli/release_cmd.py
index 398eff7..c636ac2 100644
--- a/tracks/cli/release_cmd.py
+++ b/tracks/cli/release_cmd.py
@@ -46,6 +46,10 @@ def _publish_plan_index(store: Store, run_id: str) -> dict:
 
 
 def _already_executed_ops(store: Store, run_id: str) -> list[dict]:
+    # Counterexample AC-FR0287-04: irreversible operations report none
+    del store, run_id
+    return []
+
     """AC-FR0287-04: irreversible operations (merge/tag/artifact/release)
     already executed in this run — crossing them requires an explicit
     --confirm. The operation identity joins publish.executed(done) back to
