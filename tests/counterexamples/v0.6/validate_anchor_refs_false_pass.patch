diff --git a/tracks/executor/hotfix.py b/tracks/executor/hotfix.py
index 7b07fac..6e17df5 100644
--- a/tracks/executor/hotfix.py
+++ b/tracks/executor/hotfix.py
@@ -129,7 +129,9 @@ def validate_anchor_refs(
     entries are human-locatable strings. Any miss -> verdict.failed
     (check=anchor_invalid) -> Sage redispatch (<=3, SM-01.6).
     """
-    raise NotImplementedError("IF-HOTFIX-004: validate_anchor_refs")
+    # COUNTEREXAMPLE: always returns (True, []) - claims all refs
+    # exist even when acceptance.md has no such heading (contract: miss -> False).
+    return (True, [])
 
 
 def complete_hotfix_entry(
