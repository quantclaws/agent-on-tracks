---
spec_id: SPEC-NNN
created: {YYYY-MM-DD}
status: draft
sha:
---

# {功能标题} — 需求规格

<!-- 模板指引（生成文档时阅读；填写完成后删除本注释，交付的文档只留需求内容）：
  - 职责边界：本文档只描述需求本身。产品叙事/主路径留在 story.md，「来源」只记 BS-XX / 章节 ID，
    不复制正文；验收标准写入 acceptance.md（AC-FRXXXX-YY）。
  - 条目格式：`### FR-XXXX {标题}`（FR/NFR 大写、四位补零），下接字段行，再写需求描述。
  - ID 唯一且不复用：新条目取下一个未用序号，可插入文档任意位置；废弃/被拒绝的需求直接删除条目
    （历史经 git 追溯），已用过的编号不再复用。
  - 范围上限：有效 FR 不超过 30 条（NFR 不计），超限会被 scope 门禁退回拆分。
  - 决策记录在 inline-discussion 中；只有未 resolved 的 inline-discussion 才能阻塞评审退出。
  - 例外才写：可测性有保留时加一行 `- **可测性保留**：{理由}`；无保留不写。
  - 交付入口：用户触达本需求的产品表面。有「界面与入口」章节时引用条目编号（E-01）；
    没有时直接写表面（`trac init`、`POST /api/releases`）；纯内部约束写“无独立入口，依附 FR-XXXX”。
  - 条件章节（格式复制下方骨架注释，置于「功能需求」之前；**不适用的整节不出现**）：
    * `## 界面与入口`（E-XX）—— 有用户可见表面时必加。示意性质（informative）：
      验收以 FR/AC 文字为准，视觉调整不推翻已批准的 FR。
    * `## 状态与生命周期`（SM-XX）—— 对象有用户可见状态时必加。约束性质（normative）：
      未列出的状态转移即不允许，FR 描述可直接引用 SM-XX 及其转移行号（如 SM-01.2）。
      转移清单为准；如需可另附 mermaid 图，仅作示意。
    * `## 角色与权限`（RP-XX）—— 多角色产品时必加。约束性质（normative）。
  - 一般不使用表格，因为它不利于使用 tracks-discuz skill 来评论。如果必须要使用，则表格必须增加行序号列以方便引用。
-->

<!-- 条件章节骨架（适用时复制到「功能需求」之前并填写；本注释随模板指引一并删除）

## 界面与入口

### E-01 {表面名}

```
┌──────────────────────────────────┐
│ {文本 wireframe 或 CLI           │
│  transcript 代码块，不用图片}    │
└──────────────────────────────────┘
```

## 状态与生命周期

### SM-01 {对象名}

1. （新建）→ {状态 A}：{触发动作/条件}
2. {状态 A} → {状态 B}：{触发动作/条件}
3. {状态 B} → {终态}：{触发动作/条件}

## 角色与权限

### RP-01 {范围}

| #   | 操作     | {角色 A} | {角色 B} |
| --- | -------- | -------- | -------- |
| 1   | {操作名} | ✅        | ❌        |
| 2   | {操作名} | ✅        | ✅        |

-->

## 功能需求

### FR-0001 {标题}

- **来源**：{`BS-01` / `§3.1`}
- **交付入口**：{`E-01` / `trac init` / …}

{需求描述}

## 非功能需求

### NFR-0001 {标题}

- **来源**：{`BS-02`}

{需求描述}
